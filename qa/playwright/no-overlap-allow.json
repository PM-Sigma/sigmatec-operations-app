{
  "home": "legacy kibbutz-card grid — package K rewrites KibbutzCard/KibbutzDetail onto SectionBlock/ListRow",
  "calendar": "legacy .ucal-* grid — package C rewrites Calendar.tsx onto DayCell/SegmentedControl",
  "inventory": "legacy inv-* markup — package I is the full React rewrite (06/07/08/20 + inventory index.html)",
  "attendance": "remaining findings are the SHARED shell header (bell/tasks icon buttons, \"אביאם\"/install-app bubbles, search input, Sheet's own ✕ close button) and Field's \"תיעוד ביקור\" CTA — none of them attendance's own files, which A doesn't own and doesn't touch",
  "more-sheet": "MoreSheet.tsx predates BubbleButton — package S (shell) rewrites the row/chip controls",
  "burns": "round 5 G-U2/G-U4 fixed the table's own findings (nested-interactive row); what's left is the shared inner-page header (brand text overflow + nav bubble sizes) every 'openPage' screen shows once nothing else masks it — package S owns the header/nav chrome",
  "pushlog": "new round 5 G-U3 page — same shared inner-page header/nav findings as 'burns' above (package S), nothing of pushlog's own content in the list",
  "settings-sheet": "round 5 G-U1/G-U4 fixed the sheet's own findings (native <select>/steppers, tap targets, missing labels); what's left is the home card underneath the sheet (badge-orphans contrast, a .text-destructive bdi) — package K owns EmsTasks.tsx"
}
