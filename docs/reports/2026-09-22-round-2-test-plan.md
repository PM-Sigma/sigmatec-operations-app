# תכנית בדיקות — סבב 2 מהטלפון (22.9.2026) · גרסה 2.12

מיפוי כל סעיף מהודעת עידן (22.9, 15:15) → מה נעשה → איך מוודאים. "אוטומטי" = בדיקה שרצה ב-`npm test` /
Playwright; "ידני" = צעד לבדיקה בטלפון (Galaxy S24) או בדסקטופ. Preview `dev`/`main`: הגרסה מופיעה בתחתית הבית (2.12).

## נתונים (בוצע ב-Supabase, 22.9)
| סעיף | נעשה | ווידוא |
|---|---|---|
| "החדשים היחידים: דגניה ב, עין דור, ניר עציון" | `kibbutzim.section='new'` רק לשלושתם; כל השאר `active`; דפנה שוחזרה מארכוב | ידני: דף הבית → 3 כרטיסים ב-🆕 לקוחות חדשים, דפנה ב-✅ פעילים |
| מיגרציות | `work_sessions_log`, `internal_tasks_fields`, `meter_burns_ems_task`, `visits_reason`, `company_holidays_eves` הוחלו | `select` על העמודות/הטריגר — בוצע |

## דף הבית וכללי
| # | סעיף | נעשה (חבילה) | ווידוא |
|---|---|---|---|
| 1 | כותרת חתוכה, התראות עולות עליה → כותרת בשתי שורות | "Sigmatec" / "Operations" בשתי שורות, Σ בפינה, אשכול ימני לא חופף (A) | אוטומטי `nav-shell.spec` phone · ידני: 360px אין חיתוך |
| 2 | הוספת קיבוץ ל"עוד" בלבד | הכפתור מוסתר בטלפון, קיים ב-⋯ (A) | אוטומטי `home-cards.spec` |
| כללי 1 | Back זורק מהאפליקציה → דיאלוג יציאה/דף הבית; טיוטה בטופס מלוכלך; לחיצה מחוץ לחלון נשארת | sentinel היסטוריה + "לצאת מהאפליקציה?" [חזרה לדף הבית][יציאה]; Back על טופס מלוכלך → שמור טיוטה/לצאת בלי לשמור/להמשיך (B) | אוטומטי `test-back-button.mjs` (backAction) + `pending-states.spec` (goBack ×3 מצבים) · ידני: Back מהבית, Back מטופס עם טקסט |
| כללי 2 | קיבוצים עם טיוטה פתוחה למעלה + הודעה | מדור "✍️ טיוטות פתוחות" בראש הבית (`draftsAtTop`) (A). לא נעשה: "פתיחת משימת EMS בתהליך" — אין מצב כזה בקוד | אוטומטי `kibbutzim.test` · ידני: התחל סיכום ביקור, צא, הכרטיס למעלה |
| כללי 3 | יומן במקום רעיון/באג; לאביאם/ניתאי: נוכחות·יומן·ביקור·קיבוצים, מלאי ראשון ב"עוד" | `navTabsFor(role,user)` (A) | אוטומטי `landing.test` + `nav-shell.spec` · ידני: התחבר כאביאם |

## סיכום ביקור (גיליון הפרקים = מה שהצוות משתמש בו; הטופס הישן = fallback)
| # | סעיף | נעשה (C) | ווידוא |
|---|---|---|---|
| 1 | placeholder "1.5" | בטופס הישן + תיבת הזנה ידנית בגיליון | אוטומטי `visit-form.spec` |
| 2 | להסיר מלאי מקור | מוסתר, ברירת מחדל חברה | אוטומטי `visit-form.spec`, `test-visit-cert-gate` |
| QA1 | חובה: מי ביקר, משך ותאריך, מה עשיתי, איש קשר | `missingFields()` + מסגרת אדומה "· חובה" + גלילה לראשון | אוטומטי `visitDraft.test`, `visit-chapters.spec` · ידני: שלח ריק |
| QA1/2 | מוצרים נוספים: חיפוש מילות מפתח, כינויים, טעויות כתיב; 360ct/e570/em133/pm135 מונים; סימים מוסתרים | `productSearch.ts` (נרמול, כינויים, 3–6 שגיאות למוצר, בחירת CT/PP/SP) | אוטומטי `productSearch.test` · ידני: הקלד "לנדיס", "e570", "בקר" |
| QA3 | ציוד שהוחזר: מכווץ, ➕, שורה תקינה | פאנל מכווץ, שורה 42px, `returnedItems` נשמר | אוטומטי `visit-chapters.spec` (רוחב 390) |
| QA4 | לא להניח משימת EMS; בחירה מרובה כולל פנימיות (אביאם↔ניתאי); סיבת ביקור אם אין | multi-select, `sharedOwners`, תגובה לכל משימה שנבחרה, פנימיות → done, `VISIT_REASONS` → `visits.reason` | אוטומטי `field.test` (sharedOwners, reason), `visit-chapters.spec` · ידני: סמן 2 משימות, בדוק תגובות ב-EMS |
| QA5 | אחרי שמירה עם ציוד → מסך תעודה; הפק (בלי הדפסה); שלח במייל / הורד PDF; כניסה חוזרת; עריכה → שמור עדכונים → שלח | שמירה נוחתת על מסך התעודה; `noPrint`; `certSendOpen` (בחירת איש קשר עם מייל + הוספה inline); הורדה דרך ה-overlay הקיים (הנתיב היחיד ל-PDF באפליקציה); כניסה חוזרת מהביקור/מהכרטיס/מטאב תעודות | אוטומטי `certSend.test`, `test-delivery-cert`, `visit-chapters.spec` · ידני: מסלול מלא כולל שליחת מייל אמיתי |
| לחצן 1 | 🚚 הפק תעודת משלוח למטה נעלם | חזר בתחתית הסיכום | אוטומטי `visit-form.spec` |
| הקלטה | הקלטת סיכום ביקור → ניתוח ממלא שדות, ניסיוני, טיפים, למידה מתיקונים, הדבקת טקסט | פאנל מעל הפרקים; שרשרת `parse-daylog` המשותפת עם יומן היום (`daylogChain.ts`), תיקונים ל-`daylog_corrections` | אוטומטי `field.spec` (paste path) · ידני: הקלט 20 שניות, ודא שדות מולאו ונשארו לעריכה; לעקוב אחרי עלות ב-`transcribe_log` |

## דיווח באג/רעיון (D)
| # | סעיף | נעשה | ווידוא |
|---|---|---|---|
| 1 | קריסה בסגירה + טיוטה | סיבת השורש: teardown נורה ב-mount הראשון ומחק טיוטה; עכשיו flush רק במעבר פתוח→סגור, טיוטה ב-localStorage חוזרת | אוטומטי `feedback.test` (4), `feedback.spec` "closing does not lose the text" (reload אמיתי) |
| 2 | תמלול מכפיל, מד ווליום מת → דיבור לטקסט | `parseRecognitionEvent`: interim מחליף, final מוסיף; כפתור אחד 🎤 דיבור לטקסט; הקלטה רק כ-fallback; מד הוסר | אוטומטי `speech.test` (רצף "זו זו זו בדיקה") · ידני: אמור "זאת בדיקה האם זה עובד" |
| 3 | טוגל אנונימי הפוך | תיקון ב-`switch.tsx` (RTL) לכל המתגים | אוטומטי צילום `feedback.spec` · ידני |
| 4 | להסיר "מגיע לעידן ועמיחי" | הוסר | אוטומטי `feedback.spec` |

## מודול מלאי — תעודות (E)
| # | סעיף | נעשה | ווידוא |
|---|---|---|---|
| 1 | תעודות אחרי מלאי בקיבוצים | סדר טאבים | אוטומטי `test-html-structure`, `inventory-pool.spec` |
| 2 | סינונים כמטריצה, "הכל" ברירת מחדל וחוזר בביטול, תאריכים מעוצבים | `sig-grid` chips + `certRangeTap` + `.cert-date-range` | ידני: לחץ פעמיים על "החודש" → חוזר להכל |
| 3 | דוחות לפי סינון: סיכום תקופתי + Excel; להוריד סיכום חודשי | שני כפתורים על הסינון הנוכחי; חודשי הוסר | אוטומטי `test-exports` |
| — | ✉️ שליחה מהטאב | קורא ל-`certSendOpen` (C) | ידני |

## נוכחות (F)
| # | סעיף | נעשה | ווידוא |
|---|---|---|---|
| 1 | ימים חסרים כבועות לחיצות | pills 38px + ＋ | אוטומטי `attendance.spec` (גובה יעד) |
| 2 | סיכום ביקור → יום שטח אוטומטי; עריכת תאריך מזיזה | `withVisitDays()` (גזירה, לא כתיבה) | אוטומטי 8 goldens `attendance.test` · ידני: שנה תאריך ביקור, היום הישן חוזר לחסר |
| 3 | סמלי Excel/PDF ברורים | אייקון + תווית | ידני |
| 4 | ימים חסרים באדום ביומן | `missingDaysFor` יוצא מ-F; **לא חובר ל-CalCell** (G רץ במקביל ולא מצא אותו). פתוח, שורה אחת ב-Calendar.tsx | ידני: לא יופיע עדיין |
| 5 | ערבי חג: סימון, חובה, ברירת מחדל מהבית, טיימר 4 שניות | kind `holiday_eve` (8 ערבים 2026–27), countdown + ביטול | אוטומטי `attendance.test`, `attendance.spec` · ידני: 20.9 מסומן ערב יום כיפור |
| 6 | אביאם רואה את ניתאי | `canSwitchPerson` כולל שניהם, קריאה בלבד | אוטומטי `attendance.spec` |
| 7 | אייקון נוכחות שונה מיומן | `UserCheck` (A) | ידני |

## יומן ובריפינג (G)
| # | סעיף | נעשה | ווידוא |
|---|---|---|---|
| 1 | להסיר +, שבוע עבודה ברירת מחדל, כפתור "חודש מלא"/"שבוע עבודה" | `workWeekLabel`, `visibleDows` | אוטומטי `calendar.test` (12), `calendar.spec` |
| 2 | מספרי שבוע בקצה, רוחב מלא | 14px edge | אוטומטי צילום |
| 3 | תאריך עבר: צפייה בסיכום, בלי מסלול/בריפינג | `PastDay` | אוטומטי `calendar.spec` |
| 4 | תאריך עתידי: חיפוש קיבוץ → מסלול; בריפינג עם כל המשימות בלי תאריכי יעד; להסיר "הסתר משימות EMS" | `cal-place-search`, `briefingTasks`, הטוגל הוסר | אוטומטי `calendar.spec`, `field.test` |
| 5 | בורר חודש/שבוע/רשימה ברור | `.ucal-switch-split` | אוטומטי (מדידת הפרדה) |
| ב1 | צריבות בבריפינג כקטגוריה סגורה | `BurnCategory` + `burnSummary` | אוטומטי `field.spec`, `burns.spec` |
| ב2 | בריפינג בלחיצה על יום עתידי עם שיבוץ | `cal-day-brief` | אוטומטי `calendar.spec` |
| ב3 | בריפינג של היום בכניסה הראשונה + שורה מעל הנוכחות | `briefingAutoOpen` (latch יומי) + `today-brief-row` | אוטומטי `field.test` · ידני: כניסה ראשונה ביום עם שיבוץ |

## מצב ישיבה + ישיבת פיתוח (H)
| # | סעיף | נעשה | ווידוא |
|---|---|---|---|
| 1 | רוחב טלפון; ✕ סוגר | תוקן | אוטומטי `presenter.spec`, `dev-meeting.spec` |
| 2 | משימות EMS ופנימיות כטקסט | בכל שקופית | אוטומטי `Presenter.test` |
| 3 | "ניהולי"/איזור כצ'יפים; שורה תחתונה מקצה לקצה; חצים גדולים עם שם הבא/קודם | נעשה; הצ'יפים לקריאה (אין נתיב עדכון קיים) | ידני |
| 4 | סטופר ידני | התחלה/עצירה בבחירת המשתמש | אוטומטי `meetingRun.test` |
| 5 | מאז הישיבה הקודמת | משימות פנימיות (יש חותמות זמן); EMS ללא חותמת פתיחה — לא בפיצול, כן בטקסט | אוטומטי `meetingRun.test` |

## תיעוד לשימוש ב-brag (I)
`docs/usage/` — 5 מדריכים + README + 11 צילומים. ידני: קריאה.

## whisper כמשתתף בישיבה — נענה בטקסט (ספק, לא נבנה): אפשרי עם `transcribe` + שרשרת הניתוח; סיכונים: דיוק עברית ממיקרופון טלפון, ללא זיהוי דוברים. חבילה עתידית בתוך ▶ מצב ישיבה.

## לא נעשה / פתוח
- ימים חסרים באדום על גריד היומן (F4↔G) — חיבור של שורה אחת, נשאר לסבב הבא.
- "פתיחת משימת EMS בתהליך" כמפתח מיון בבית — אין מצב כזה בקוד.
- דחיפת שרת לטיימר שעתיים כשהטלפון סגור (מסבב 1).
- humanizer: ~180 מחרוזות עם קו מפריד ב-`app/src`/`js/src` (דוח סבב 1).

## תוצאות הריצה (ממולא בסיום)
- `npm test`: 50 legacy runners + vitest — ירוק (2.12).
- Playwright 4 פרויקטים: ראה סעיף התוצאות ב-CHANGELOG [2.12].
